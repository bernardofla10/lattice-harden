# Security rationale: test-only tooling is separated from runtime dependencies
# so the production image does not carry unnecessary packages or attack surface.
-r requirements.txt
httpx==0.28.1
pytest==8.3.4
